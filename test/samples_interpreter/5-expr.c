int FXD3J[4] = {0, 3, 2, 3};
int CR400BF[3][2] = {5, 0, 3, 3, 0, 5};
int pantograph_lowering_operation(int train_no[], double from, double to)
{
        return FXD3J[2 + train_no[2]];
}
int axletree_temp_high(int train_model[2])
{
        return FXD3J[0];
}
void fv()
{
	int i = 5;
	// For void function, a `ret` instruction should be filled even if there is not `return` statement
	return;
}
int main()
{
        int K1806[5] = {7, 5, 0, 3, 8};
        const double K = 1000.0;
        float a,  m, n;
        double b = 50.33;

        pantograph_lowering_operation(K1806, 64.0 * K + 520.0, 64.0 * K + 120.0)
                * 5033 + 05;

        (5.0f < a && b <= 7.0) || m == n && K1806[1] == 7;

        axletree_temp_high(CR400BF[K1806[2]]);

	return 5;
}
