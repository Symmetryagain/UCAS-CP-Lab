# PR003-CACT-实验说明与指导 

## 实验指导 

关于如何生成 RV64 汇编代码，大家可上互联网查阅资料，也可以自行编写小程序通过查看 RV64 的 
GCC（用 -S 选项）生成的汇编代码来学习。
RV64I 是 RISC-V 64 位的基本整数指令集，包含基本的算术、逻辑、控制流和内存访问指令。本实验在
此基础上还需要 M（增加了整数乘除法指令）、F（增加了单精度浮点指令）、D（增加了双精度浮点指
令）这几部分的扩展指令，以支持定点乘除和浮点运算。
汇编程序基本格式 
下面以一个 CACT 小程序为例来简单说明汇编程序需要哪几个部分：

```c
int CR = 95306, CR0 = 0;
const int CR1 = 10086;
int ucas() {
return CR;
}
int main() {
int u;
u = ucas() + 1;
return u;
}
```

这个程序包含一个已初始化的全局变量 CR，一个初始化为零的全局变量
全局变量 CR1，一个普通函数
ucas() 以及
CR0 ，一个声明为
const 的
main() 函数。

使用 CACT 编译器编译后得到的汇编程序
应该由下面几个部分组成。
首先是固定的开头，这部分大家直接写定在编译器里即可：

```asm
.option nopic
.attribute  arch, "rv64i2p0_m2p0_a2p0_f2p0_d2p0_c2p0"
.attribute  unaligned_access, 0
.attribute  stack_align, 16
```

接下来是数据区，它描述了程序的 3 个全局变量。例如
CR ，其大小为 4 字节，内容为一个 32 位整数

```asm
95306 ：
.data
.align 2
.size   CR, 4
CR:
.word   95306
.section .rodata
.align 2
.size   CR1, 4
CR1:
.word   10086
.bss
.align 2
.lcomm CR0, 4
```

一般而言，已初始化的非零全局变量置于
.data 节，未初始化或初始化为零的全局变量置于
.bss
节，声明为
const 的全局变量置于
.rodata 节。
然后到了代码部分，即
.text ，其开头可写定为：
.text
.align  1
随后是
ucas() 和
main() 这两个函数的定义和指令序列，它们均由.global 和 
.type 开头的两行
以及以函数名命名的标签作为起始，并且以返回指令ret 结尾：

```asm
.globl  ucas
.type   ucas, @function
ucas:
addi    
sp, sp, -16
sd  ra, 8(sp)
lw  t0, CR
mv  a0, t0
j   
.Lf7L1
.Lf7L1:
ld  ra, 8(sp)
addi    
sp, sp, 16
ret
.globl  main
.type   main, @function
main:
addi    
sp, sp, -16
sd  ra, 8(sp)
call    
ucas
addi    
t0, a0, 1
mv  a0, t0
j    .Lf8L3

.Lf8L3:
ld  ra, 8(sp)
addi    
sp, sp, 16
ret
```
